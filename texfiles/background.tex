\section{Background}\label{sec:background}

\begin{itemize}
    \item Learned policies can be significantly better than stock OS {\it on average}. Demonstrate with examples.
    \item However, sometimes the models may misbehave - in particular, they do not come with any guarantees. Standardized API; taxonomy.
    \item Examples of the type of guarantees that are needed in OSes.
    \item Coming up with these guarantees is easier than the full algorithm.
\end{itemize}
\mike{Can we add some citations on bad behavior here?}
\mike{Add an analogy to driverless cars: Just as driverless cars may be safer than human drivers but are held to high standards due to their autonomy, learned policies may be safer then the error-prone heuristics developers are currently comfortable with.}

We want to build a standardized framework that has consistent usage across multiple learned policies. 

\jane{Design goals: modularity; expressiveness; low overhead; robustness (after applying corrective actions)}

R1. Expressiveness: allow high-level system state and specific properties for models (L)

R2. Framework Modularity: Plug and play for properties (probably covers extensibility) (A)

R3. Lightweight / low overhead (I)

R4. Checkability 

R5. Safety (or crash-free) (I)
